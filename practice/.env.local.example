# Convex deployment configuration
EXPO_PUBLIC_CONVEX_URL=your_convex_deployment_url
EXPO_PUBLIC_CONVEX_SITE_URL=your_convex_site_url

# Dev Bypass (set to 'true' to bypass authentication for development)
# NOTE: There's a discrepancy - frontend uses EXPO_PUBLIC_DEV_BYPASS_AUTH, backend uses DEV_BYPASS_AUTH
# For full bypass, you need to set BOTH variables to true:
#   - EXPO_PUBLIC_DEV_BYPASS_AUTH=true (for frontend)
#   - DEV_BYPASS_AUTH=true (for Convex backend - set in Convex dashboard)
EXPO_PUBLIC_DEV_BYPASS_AUTH=false

# AI Service timeouts
EXPO_PUBLIC_AI_ANALYSIS_TIMEOUT_MS=60000
EXPO_PUBLIC_AI_PROCESSING_NOTIFICATION_MS=15000

# Video configuration
EXPO_PUBLIC_MAX_VIDEO_DURATION_SECONDS=60
EXPO_PUBLIC_MAX_VIDEO_FILE_SIZE_MB=150

# Convex backend environment variables (set in Convex dashboard, NOT in this file)
# CONVEX_SITE_URL=https://your-deployment.convex.site
# DEV_BYPASS_AUTH=false
# GEMINI_API_KEY=your_gemini_api_key
